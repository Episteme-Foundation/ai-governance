AWSTemplateFormatVersion: '2010-09-09'
Description: 'Langfuse - Self-hosted LLM Observability'

Parameters:
  Environment:
    Type: String
    Default: production
    AllowedValues:
      - production
      - staging

  DatabaseStackName:
    Type: String
    Default: ai-governance-database
    Description: Name of the database CloudFormation stack

  # Note: ECR repository 'langfuse' must have an image before deploying
  # Use the deploy-langfuse GitHub Actions workflow to mirror from Docker Hub

  NextAuthSecret:
    Type: String
    NoEcho: true
    Description: Random string for NextAuth session encryption (min 32 characters)
    MinLength: 32

  Salt:
    Type: String
    NoEcho: true
    Description: Random string for hashing (min 32 characters)
    MinLength: 32

Resources:
  # IAM Role for App Runner to access ECR
  LangfuseAccessRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: langfuse-apprunner-access
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: build.apprunner.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSAppRunnerServicePolicyForECRAccess

  # IAM Role for Langfuse instance
  LangfuseInstanceRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: langfuse-apprunner-instance
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: tasks.apprunner.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - PolicyName: CloudWatchLogs
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - logs:CreateLogGroup
                  - logs:CreateLogStream
                  - logs:PutLogEvents
                Resource: '*'
        - PolicyName: SecretsManagerAccess
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - secretsmanager:GetSecretValue
                Resource: !Sub 'arn:aws:secretsmanager:${AWS::Region}:${AWS::AccountId}:secret:langfuse/*'

  # Store Langfuse secrets
  LangfuseSecret:
    Type: AWS::SecretsManager::Secret
    Properties:
      Name: langfuse/config
      Description: Langfuse configuration secrets
      SecretString: !Sub |
        {
          "NEXTAUTH_SECRET": "${NextAuthSecret}",
          "SALT": "${Salt}"
        }

  # Langfuse App Runner Service
  LangfuseService:
    Type: AWS::AppRunner::Service
    Properties:
      ServiceName: langfuse
      SourceConfiguration:
        AuthenticationConfiguration:
          AccessRoleArn: !GetAtt LangfuseAccessRole.Arn
        AutoDeploymentsEnabled: false
        ImageRepository:
          # Image mirrored from Docker Hub via GitHub Actions workflow
          ImageIdentifier: !Sub '${AWS::AccountId}.dkr.ecr.${AWS::Region}.amazonaws.com/langfuse:latest'
          ImageRepositoryType: ECR
          ImageConfiguration:
            Port: '3000'
            RuntimeEnvironmentVariables:
              - Name: NODE_ENV
                Value: !Ref Environment
              # Database connection - imports from database stack
              - Name: DATABASE_URL
                Value:
                  Fn::Sub:
                    - 'postgresql://postgres:${Password}@${Host}:5432/langfuse'
                    - Host:
                        Fn::ImportValue: !Sub '${DatabaseStackName}-DBEndpoint'
                      Password: '{{resolve:secretsmanager:ai-governance/database:SecretString:password}}'
              # NextAuth configuration
              - Name: NEXTAUTH_SECRET
                Value: !Ref NextAuthSecret
              - Name: SALT
                Value: !Ref Salt
              # Will be updated to actual URL after first deployment
              - Name: NEXTAUTH_URL
                Value: 'https://placeholder.awsapprunner.com'
              # Telemetry disabled
              - Name: TELEMETRY_ENABLED
                Value: 'false'
              # Enable public sign-up for initial setup, disable after
              - Name: AUTH_DISABLE_SIGNUP
                Value: 'false'
      InstanceConfiguration:
        Cpu: '1024'
        Memory: '2048'
        InstanceRoleArn: !GetAtt LangfuseInstanceRole.Arn
      HealthCheckConfiguration:
        Protocol: HTTP
        Path: /api/public/health
        Interval: 20
        Timeout: 10
        HealthyThreshold: 1
        UnhealthyThreshold: 5

Outputs:
  LangfuseUrl:
    Description: Langfuse dashboard URL
    Value: !Sub 'https://${LangfuseService.ServiceUrl}'
    Export:
      Name: !Sub '${AWS::StackName}-LangfuseUrl'

  LangfuseApiUrl:
    Description: Langfuse API base URL (use in LANGFUSE_BASE_URL)
    Value: !Sub 'https://${LangfuseService.ServiceUrl}'
    Export:
      Name: !Sub '${AWS::StackName}-LangfuseApiUrl'

  ECRRepositoryUri:
    Description: ECR repository URI for Langfuse image
    Value: !Sub '${AWS::AccountId}.dkr.ecr.${AWS::Region}.amazonaws.com/langfuse'
    Export:
      Name: !Sub '${AWS::StackName}-ECRRepositoryUri'

  SetupInstructions:
    Description: Post-deployment steps
    Value: |
      1. Update NEXTAUTH_URL environment variable to the actual LangfuseUrl
      2. Create a Langfuse user via the web UI
      3. Create an API key pair in Langfuse settings
      4. Add LANGFUSE_PUBLIC_KEY, LANGFUSE_SECRET_KEY, LANGFUSE_BASE_URL to ai-governance/app-config secret
      5. Redeploy ai-governance to pick up the new Langfuse configuration
