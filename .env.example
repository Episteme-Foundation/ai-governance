# Database Configuration
# For local development, use localhost
# For production, use your RDS endpoint (from AWS Secrets Manager)
DATABASE_URL=postgresql://postgres:password@localhost:5432/ai_governance?ssl=true&sslmode=no-verify

# AWS Configuration (for deployment and Secrets Manager)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# Anthropic API (required - for Claude agent execution)
ANTHROPIC_API_KEY=

# OpenAI API (required - for decision embeddings and semantic search)
OPENAI_API_KEY=

# GitHub App Configuration (required for autonomous governance)
# The GitHub App enables agents to review PRs, approve, merge, and comment
# Without this, agents cannot interact with GitHub autonomously
GITHUB_APP_ID=
GITHUB_APP_PRIVATE_KEY_PATH=
GITHUB_WEBHOOK_SECRET=
GITHUB_REPOSITORY=owner/repo

# Redis Configuration (optional - for rate limiting, not yet implemented)
REDIS_URL=redis://localhost:6379

# Langfuse Configuration (optional - for LLM observability and self-improvement)
# Self-hosted: Deploy with infra/scripts/deploy-langfuse.sh
# Cloud: Use https://cloud.langfuse.com
# When enabled, agents can query their own traces for self-improvement
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_BASE_URL=https://cloud.langfuse.com

# Server Configuration
PORT=3000
NODE_ENV=development
